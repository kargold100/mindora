/* ====================================================
   MINDORA — config.js
   Set APPS_SCRIPT_URL to your deployed Apps Script Web App URL
   to enable cross-device profiles (see /apps-script/Code.gs and
   the README for deployment steps).

   Leave this empty ('') and Mindora runs in LOCAL MODE: profiles
   still work, but are stored only in this browser and won't
   follow you to another device. This is the safe default so the
   app works immediately, even before you've deployed a backend.
   ==================================================== */
const APPS_SCRIPT_URL = '';
